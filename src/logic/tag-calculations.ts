import { TFile } from "obsidian";



export const refreshTagSuggestions = (file: TFile) => {
    console.log('refreshing tags');
    console.log('file', file);

    // Read file

    // Map all words

    // Filter out language model's ignored words

    // Filter out user's ignored words

    // Order by most used
}